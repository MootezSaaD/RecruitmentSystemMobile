<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.medtech.recruitmentsystemapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.ApplicantJobDetailActivity"></activity>
        <activity android:name=".ui.RecruiterJobOfferDetailsActivity"></activity>
        <activity android:name=".ui.ApplicantDashboardActivity" android:theme="@style/AppTheme.NoActionBar" />
<!--        Remove Action bar for the RecruiterDashBoardActivity-->
        <activity android:name=".ui.RecruiterDashboardActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.CreateJobFragment" />
        <activity android:name=".ui.ApplicantRegisterActivity" />
        <activity android:name=".ui.RecruiterRegisterActivity" />
        <activity android:name=".ui.DummyActivity" />
        <activity android:name=".ui.GeneralRegisterActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>